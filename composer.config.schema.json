{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://composer.dev/schemas/composer.config.schema.json",
  "title": "Composer Configuration",
  "description": "Dependency Inversion at runtime: maps subagent role → provider impl. C0.2 — frozen Wave 0, append-only during Wave 1.",
  "type": "object",
  "required": [
    "roles"
  ],
  "additionalProperties": false,
  "properties": {
    "roles": {
      "type": "object",
      "required": [
        "researcher",
        "coder",
        "reviewer"
      ],
      "additionalProperties": false,
      "properties": {
        "researcher": {
          "$ref": "#/$defs/roleConfig"
        },
        "coder": {
          "$ref": "#/$defs/roleConfig"
        },
        "reviewer": {
          "$ref": "#/$defs/roleConfig"
        },
        "reviewerClaude": {
          "$ref": "#/$defs/roleConfig"
        },
        "coderCli": {
          "$ref": "#/$defs/roleConfig"
        }
      }
    },
    "spendAuthorization": {
      "$ref": "#/$defs/spendAuthorization"
    },
    "codexReview": {
      "$ref": "#/$defs/codexReview"
    }
  },
  "$defs": {
    "codexReview": {
      "type": "object",
      "required": [
        "enabled"
      ],
      "additionalProperties": false,
      "description": "Optional Codex review-gate settings. Composer owns the trigger points; omit or disable to keep review-gate off.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Master opt-in switch. false keeps Codex review-gate shipped off."
        },
        "triggers": {
          "type": "object",
          "additionalProperties": false,
          "description": "Composer trigger points that may request Codex review.",
          "properties": {
            "preCommit": {
              "type": "boolean",
              "description": "Run Codex review before commit when enabled."
            },
            "postPlan": {
              "type": "boolean",
              "description": "Run Codex review after planning when enabled."
            }
          }
        },
        "preCommitCommand": {
          "type": "string",
          "enum": [
            "review",
            "adversarial-review"
          ],
          "description": "Codex companion verb for pre-commit review. Omitted = review."
        },
        "postPlanCommand": {
          "type": "string",
          "enum": [
            "review",
            "adversarial-review"
          ],
          "description": "Codex companion verb for post-plan review. Omitted = adversarial-review."
        },
        "mode": {
          "type": "string",
          "enum": [
            "ask",
            "auto"
          ],
          "description": "ask: require user confirmation before invoking Codex. auto: invoke without asking. Omitted = ask."
        },
        "execution": {
          "type": "string",
          "enum": [
            "foreground",
            "background"
          ],
          "description": "foreground: wait for Codex review. background: launch review asynchronously. Omitted = background."
        },
        "scope": {
          "type": "string",
          "enum": [
            "auto",
            "working-tree",
            "branch"
          ],
          "description": "auto: infer review scope. working-tree: review local changes. branch: review against base. Omitted = auto."
        },
        "base": {
          "type": "string",
          "minLength": 1,
          "description": "Base branch for branch-scoped review. Omitted = main."
        },
        "preCommitHook": {
          "$ref": "#/$defs/codexPreCommitHook"
        }
      }
    },
    "codexPreCommitHook": {
      "type": "object",
      "required": [
        "enabled"
      ],
      "additionalProperties": false,
      "description": "Optional mechanical PreToolUse gate for git commit. Omitted or disabled = off.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Opt-in switch for the mechanical pre-commit gate."
        },
        "blockOnSeverity": {
          "type": "string",
          "enum": [
            "critical",
            "high",
            "medium",
            "low"
          ],
          "description": "Minimum finding severity that blocks commit. Omitted = high."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "description": "Review wall-clock timeout in milliseconds. Omitted = 120000."
        },
        "failClosed": {
          "type": "boolean",
          "description": "If true, reviewer unavailability blocks commit. Omitted = false."
        }
      }
    },
    "spendAuthorization": {
      "type": "object",
      "required": [
        "mode"
      ],
      "additionalProperties": false,
      "description": "Orchestrator behaviour when a real-money provider call is about to be made. Added 2026-05-24 after the first dogfood audit revealed the soft 'always ask' rule was inconsistent across sessions. Optional — omit for the default 'interactive' behaviour.",
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "interactive",
            "auto",
            "deny"
          ],
          "description": "interactive: ask the user 'go' before any priced call (legacy default). auto: proceed without asking, respect the maxUsd caps. deny: refuse all real-money calls; force tape replay / mock providers."
        },
        "maxUsdPerSession": {
          "type": "number",
          "minimum": 0,
          "description": "Hard cap on accumulated USD spend across the whole orchestrator session. 0 = no spend; omitted = no cap."
        },
        "maxUsdPerCall": {
          "type": "number",
          "minimum": 0,
          "description": "Hard cap on a single provider call's USD estimate. 0 = block everything; omitted = no cap."
        }
      }
    },
    "providerId": {
      "type": "string",
      "enum": [
        "anthropic",
        "openai_compatible",
        "cli",
        "mock"
      ],
      "description": "Discriminator picked up by ProviderFactory. Must match IProvider.id."
    },
    "roleConfig": {
      "type": "object",
      "required": [
        "provider"
      ],
      "additionalProperties": false,
      "properties": {
        "provider": {
          "$ref": "#/$defs/providerId"
        },
        "apiKeyEnv": {
          "type": "string",
          "description": "Name of the env var holding the API key. Resolved by ProviderFactory at startup; the key value itself is NEVER stored in this file."
        },
        "baseUrl": {
          "type": "string",
          "format": "uri",
          "description": "HTTP base URL for SDK-based providers (e.g. GLM Anthropic-compat endpoint)."
        },
        "model": {
          "type": "string",
          "description": "Provider-specific model identifier."
        },
        "cli": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "argv array for CLIProvider, e.g. [\"codex\",\"exec\",\"--sandbox\",\"workspace-write\",\"-c\",\"approval_policy=\\\"never\\\"\"] for coding, [\"codex\",\"--search\",\"--ask-for-approval\",\"never\",\"exec\",\"--sandbox\",\"read-only\"] for research, or [\"agy\",\"--dangerously-skip-permissions\",\"-p\"] for agy. The prompt is appended as the final argv item — NEVER shell-interpolated. For `codex exec`, including Codex global flags before `exec`, CLIProvider auto-captures the final message with --output-last-message unless the flag is already present."
        },
        "maxTokens": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-role override for the provider's default max output tokens. When `thinking.budgetTokens` is set this must be greater than it. Added 2026-05-25 for v0.1.3."
        },
        "thinking": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type"
          ],
          "description": "Extended-thinking knob for Anthropic-compat providers (GLM glm-5.1, glm-4.6+, Claude). Ignored by other providers. Added 2026-05-25 for v0.1.3.",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "enabled",
                "disabled"
              ],
              "description": "enabled: include the `thinking` block on every call. disabled: explicitly opt out."
            },
            "budgetTokens": {
              "type": "integer",
              "minimum": 1024,
              "description": "Max tokens the model may spend on internal reasoning. Required when type=enabled. Anthropic SDK minimum is 1024."
            }
          }
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "description": "CLIProvider wall-clock timeout in milliseconds for one attempt. Ignored by non-CLI providers."
        },
        "maxBuffer": {
          "type": "integer",
          "minimum": 1,
          "description": "CLIProvider stdout/stderr buffer cap in characters before the child process is terminated. Ignored by non-CLI providers."
        },
        "retries": {
          "type": "integer",
          "minimum": 0,
          "description": "CLIProvider retry count after transient failures. Use 0 for fail-fast dispatches such as reviewers."
        },
        "maxResultChars": {
          "type": "integer",
          "minimum": 0,
          "description": "CLIProvider result projection cap in characters. Set 0 to disable result projection. Ignored by non-CLI providers."
        }
      }
    }
  }
}
