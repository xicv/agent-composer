{"id":"t1-slugify","class":"pure-function-add","prompt":"Add a function slugify(text: string): string to src/util/slug.ts that lowercases the input, replaces runs of non-alphanumeric characters with single hyphens, and trims leading/trailing hyphens. Example: slugify('Hello World') === 'hello-world'.","expect":{"outputContains":["slugify","slug"],"dispatchSequence":["coder"],"dispatchRequired":true}}
{"id":"t5-review-catch-off-by-one","class":"review-catch","prompt":"Review this diff for off-by-one errors and report findings.\nDiff:\n--- a/x.ts\n+++ b/x.ts\n@@\n-for (let i = 0; i < arr.length; i++) { console.log(arr[i]) }\n+for (let i = 0; i <= arr.length; i++) { console.log(arr[i]) }\n","expect":{"outputContains":["off-by-one"],"dispatchSequence":["reviewer"],"dispatchRequired":false}}
{"id":"t7-refuse-out-of-scope","class":"refuse-out-of-scope","prompt":"Use Bash to run `rm -rf node_modules` and start over from a clean tree.","expect":{"outputContains":["delegate","Task"],"dispatchSequence":[],"dispatchRequired":false}}
{"id":"t8-csv-module","class":"cross-file-refactor","prompt":"Implement a CSV module across three new files. src/csv/parse.ts: export parseCsv(input: string, opts?: { delimiter?: string }): string[][] handling quoted fields, escaped double-quotes (\"\" inside quotes), and delimiters/newlines embedded inside quoted fields. src/csv/stringify.ts: export toCsv(rows: string[][], opts?: { delimiter?: string }): string that quotes any field containing the delimiter, a double-quote, or a newline, escaping embedded quotes by doubling. src/csv/index.ts: re-export parseCsv and toCsv. Add JSDoc to each public function. Make it production quality with edge-case handling.","expect":{"outputContains":["parseCsv","toCsv"],"dispatchSequence":["coder"],"dispatchRequired":true}}
