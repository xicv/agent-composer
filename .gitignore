# Secrets — runtime config holding GLM creds. NEVER commit.
.env
.env.json
.env.*.local

# Node / TypeScript build artifacts
node_modules/
dist/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# Autoresearch — drafts only, never live
*.candidate.md
.claude/skills/**/proposals/

# Antigravity / agy CLI local state
.antigravitycli/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Local dev-mode overrides (bypass flag, extra tool allows)
.claude/settings.local.json

# Auto-appended by learn.sh Stop hook — keeps PII out of commits
.claude/learnings/2026-*.md
.claude/worktrees/

# Composer brief scratchpad (per-run worker dispatch maps)
.composer/briefs/
.composer/goals/
.composer/handoffs/
.composer/codex-lifecycle/

# local session memory (never commit)
AGENTS.md
HANDOFF.md
.composer/oracle/
.composer/results/
.gproj/
