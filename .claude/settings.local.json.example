{
  "_note": "Copy to .claude/settings.local.json to enable dispatch_guard Phase 2 hints. It logs Task/Agent dispatches, emits deterministic dispatch-hint additionalContext (tier/size/reasoning/recommendDispatch) computed by src/cli/dispatch-hint.ts, and never denies except the existing destructive-tiny rule.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/scripts/dispatch_guard.sh"
          }
        ]
      }
    ]
  }
}
